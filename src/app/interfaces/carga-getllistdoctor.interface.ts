import { Lista } from "../models/doctor.module";



export interface cargaGetlistdoctor{
 /*  map(arg0: (resp: any) => { nombre: any; codigo: any; }): any;
  ok:string,
lista:Lista[];
} */

lista:Lista[];
}